#[cfg(test)]
mod tests {
    use crate::strength::ship::buoyancy_load::frame::Frame;


    #[test]
    fn area_by_draft_test() {
        let drafts = 
        let areas = 
        let volumes = 
        let masses = 
        let abscissa
        let frame = Frame::new(0, drafts, areas, volumes, masses, abscissa);
        
    }

}